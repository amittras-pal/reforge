- [ ] I wonder if it's a good idea to allow editing a session log from the camendar. At least for today and yesterday, which we already to start. — Not implemented: left open, since this is a bigger architectural question (F-00's session logs are intentionally immutable except notes/RPE) rather than a quick fix. Happy to scope this into its own spec if you want to pursue it. REQUIRE NEW SPEC.

- Additional sets for an active exercise
